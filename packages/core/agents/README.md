# NEXUS Agents (registry)

Brain-grounded agent definitions installable via `nexus agent install <name>`.
The core four (implementer, test-writer, reviewer, doc-keeper) also ship
embedded in the CLI — these copies serve non-CLI consumers and future packs.

Format: see `shared/nexus-agent-authoring.md` and the CLI docs.
